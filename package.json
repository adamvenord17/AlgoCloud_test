{
  "name": "app",
  "version": "0.0.0",
  "scripts": {
    "build:production": "cd backend && npm install && npm run build",
    "start:production": "cd frontend && npm install && npm run build:production && npm run start:production"
  }
}
